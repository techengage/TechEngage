jQuery(document).ready(function() {
	
	/* Home Service Section */
	wp.customize.section( 'sidebar-widgets-sidebar-service' ).panel('homepage_setting');
	wp.customize.section( 'sidebar-widgets-sidebar-service' ).priority( '3' );

	
});